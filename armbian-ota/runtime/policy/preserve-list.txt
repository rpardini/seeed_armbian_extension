# OTA local configuration preserve whitelist (absolute paths)
# Lines starting with # are comments.
#
# Keep OTA policy files themselves across OTA.
/etc/armbian-ota/preserve-list.txt
/etc/armbian-ota/persist-map.txt

# Keep account databases across OTA. These files must remain normal files at
# runtime because useradd/groupadd replace them via rename.
/etc/passwd
/etc/shadow
/etc/group
/etc/gshadow
/etc/subuid
/etc/subgid

# Keep OTA home persist data across recovery OTA. In recovery mode /userdata is
# a rootfs directory, so it would be wiped by the rootfs rewrite. A/B mode keeps
# .persist on the separate armbi_usrdata partition, which is never rewritten.
/userdata/.persist

# Keep root/user first-login customizations.
/root/.ssh
/etc/default/useradd
/etc/adduser.conf
/etc/default/locale
/etc/locale.gen
/etc/timezone
/etc/localtime
/etc/default/keyboard
/etc/environment

# Keep stable device identity and local name settings.
/etc/hostname
/etc/hosts
/etc/machine-id

# Keep SSH host identity so clients do not see a host key change after OTA.
# Wildcards are intentionally not used because the preserve helper rejects them.
/etc/ssh

# Keep first-login generated network settings.
/etc/netplan
/etc/network/interfaces
/etc/network/interfaces.d
/etc/NetworkManager/NetworkManager.conf
/etc/NetworkManager/conf.d
/etc/NetworkManager/system-connections
/etc/systemd/network
/etc/systemd/resolved.conf
/etc/systemd/resolved.conf.d
/etc/resolv.conf
/etc/wpa_supplicant

# Keep local hardware/kernel policy that is often board-specific.
/etc/udev/rules.d
/etc/modules
/etc/modules-load.d
/etc/modprobe.d
/etc/sysctl.conf
/etc/sysctl.d

# Keep desktop login settings.
/etc/lightdm/lightdm.conf.d/22-armbian-autologin.conf
/etc/gdm3/custom.conf
/etc/sddm.conf.d/autologin.conf
/var/lib/AccountsService/users
/var/lib/AccountsService/icons
